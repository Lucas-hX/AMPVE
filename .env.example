# Only a path; the JSON contains the secret and stays outside this repository.
AMPVE_CONFIG=/home/ampve/.config/ampve/platform.json
# Set only for isolated automated tests, never for production:
# AMPVE_TESTING=1
